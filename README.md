# E-commerce Backend API

A robust RESTful API built with FastAPI to manage e-commerce operations, featuring secure user authentication and product inventory management.

## 🚀 Key Features
- User Authentication: Secure Signup and Login functionality using JWT (JSON Web Tokens).
- Password Security: Implements Bcrypt hashing to ensure user passwords are never stored in plain text.
- **Product Management: Full CRUD (Create, Read, Update, Delete) operations for managing a product catalog.
- Database Integration: Relational data management using PostgreSQL and SQLAlchemy ORM.
- Data Integrity: Strict input validation and response shaping using Pydantic models.
- Interactive API Docs: Automatic documentation and testing interface via Swagger UI.

## 🛠️ Tech Stack
- Framework: FastAPI
- Database: PostgreSQL
- ORM: SQLAlchemy
- Authentication: JWT, Passlib (Bcrypt)
